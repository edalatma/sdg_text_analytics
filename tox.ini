[tox]
envlist = py311
skipsdist=True
deps = 
    spacy
    pandas
    nltk
    ipykernel
    seaborn
    pyldavis
    gensim
    plotly
    ipywidgets
    scikit-learn

[testenv]
deps =
    {[tox]deps}

commands =
    python -m unittest discover
